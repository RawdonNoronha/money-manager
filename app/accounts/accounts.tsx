"use client";
import { FormEvent, useEffect, useState } from "react";
import { createAccount, getAccounts } from "@/lib/firestore";
import type { Account } from "@/lib/types";
const money = (value: number) => new Intl.NumberFormat("en-IE", { style: "currency", currency: "EUR" }).format(value);
export default function Accounts() { const [items, setItems] = useState<Account[]>([]); const [open, setOpen] = useState(false); const load = () => getAccounts().then(setItems); useEffect(() => { load().catch(() => undefined); }, []); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); await createAccount({ name: String(form.get("name")), type: String(form.get("type")), balance: Number(form.get("balance")), currency: "EUR" }); setOpen(false); await load(); } return <div><div className="mb-7 flex items-end justify-between"><div><h1 className="text-2xl font-bold">Accounts</h1><p className="mt-1 text-sm text-[var(--subtle)]">Balances are stored alongside your account references.</p></div><button onClick={() => setOpen(true)} className="h-10 rounded-xl bg-[#295b55] px-4 text-sm font-semibold text-white">Add account</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{items.length ? items.map((item) => <div key={item.id} className="surface p-5"><p className="font-semibold">{item.name}</p><p className="mt-1 text-xs text-[var(--subtle)]">{item.type}</p><p className="mt-5 text-xl font-bold">{money(item.balance)}</p></div>) : <div className="surface p-8 text-sm text-[var(--subtle)]">No accounts yet. Add a bank, cash, savings, or credit account.</div>}</div>{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"><form onSubmit={save} className="w-full max-w-md rounded-3xl bg-[var(--surface)] p-6"><h2 className="mb-5 text-xl font-bold">Add account</h2><div className="grid gap-4"><label><span className="field-label">Name</span><input name="name" required className="field-input" /></label><label><span className="field-label">Type</span><select name="type" className="field-input"><option>Bank Account</option><option>Cash</option><option>Savings</option><option>Credit Card</option></select></label><label><span className="field-label">Opening balance</span><input name="balance" type="number" step="0.01" defaultValue="0" className="field-input" /></label></div><div className="mt-6 flex gap-3"><button type="button" onClick={() => setOpen(false)} className="h-10 flex-1 rounded-xl border border-[var(--line)]">Cancel</button><button className="h-10 flex-1 rounded-xl bg-[#295b55] text-sm font-semibold text-white">Create account</button></div></form></div>}</div>; }
