"use client";
import { FormEvent, useEffect, useState } from "react";
import { createBudget, getBudgets, getCategories, getTransactions } from "@/lib/firestore";
import type { Budget, Category, Transaction } from "@/lib/types";
const money = (value: number) => new Intl.NumberFormat("en-IE", { style: "currency", currency: "EUR" }).format(value);
export default function Budgets() { const [items, setItems] = useState<Budget[]>([]); const [categories, setCategories] = useState<Category[]>([]); const [transactions, setTransactions] = useState<Transaction[]>([]); const [open, setOpen] = useState(false); const load = () => Promise.all([getBudgets(), getCategories(), getTransactions()]).then(([b, c, t]) => { setItems(b); setCategories(c); setTransactions(t); }); useEffect(() => { load().catch(() => undefined); }, []); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); await createBudget({ categoryId: String(form.get("categoryId")), amount: Number(form.get("amount")) }); setOpen(false); await load(); } return <div><div className="mb-7 flex items-end justify-between"><div><h1 className="text-2xl font-bold">Budgets</h1><p className="mt-1 text-sm text-[var(--subtle)]">Spent amounts are calculated from expense transactions.</p></div><button onClick={() => setOpen(true)} className="h-10 rounded-xl bg-[#295b55] px-4 text-sm font-semibold text-white">Add budget</button></div><div className="grid gap-4 sm:grid-cols-2">{items.length ? items.map((item) => { const spent = transactions.filter((t) => t.type === "expense" && t.categoryId === item.categoryId).reduce((sum, t) => sum + t.amount, 0); const percent = item.amount ? Math.min(100, Math.round(spent / item.amount * 100)) : 0; return <div key={item.id} className="surface p-5"><div className="flex justify-between"><h2 className="font-semibold">{categories.find((c) => c.id === item.categoryId)?.name ?? "Unknown category"}</h2><strong>{percent}%</strong></div><p className="mt-2 text-sm text-[var(--subtle)]">{money(spent)} spent of {money(item.amount)}</p><div className="mt-4 h-2 rounded-full bg-[var(--muted)]"><div className="h-full rounded-full bg-[#295b55]" style={{ width: `${percent}%` }} /></div><p className="mt-2 text-right text-xs text-[var(--subtle)]">Remaining {money(Math.max(0, item.amount - spent))}</p></div>; }) : <div className="surface p-8 text-sm text-[var(--subtle)]">No budgets yet. Add a category budget to track progress.</div>}</div>{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"><form onSubmit={save} className="w-full max-w-md rounded-3xl bg-[var(--surface)] p-6"><h2 className="mb-5 text-xl font-bold">Add budget</h2><div className="grid gap-4"><label><span className="field-label">Category</span><select name="categoryId" required className="field-input">{categories.filter((c) => c.type === "expense").map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label><span className="field-label">Budget amount</span><input name="amount" type="number" min="0" step="0.01" required className="field-input" /></label></div><div className="mt-6 flex gap-3"><button type="button" onClick={() => setOpen(false)} className="h-10 flex-1 rounded-xl border border-[var(--line)]">Cancel</button><button className="h-10 flex-1 rounded-xl bg-[#295b55] text-sm font-semibold text-white">Create budget</button></div></form></div>}</div>; }
